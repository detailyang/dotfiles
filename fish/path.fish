set -x PATH ~/.bin $PATH
set -x PATH ~/.cargo/bin $PATH
set -x PATH ~/.go/bin $PATH

set -x PATH ~/bin $PATH
set -x PATH ~/cargo/bin $PATH
set -x PATH ~/go/bin $PATH
set -x PATH ~/python/bin $PATH
set -x PATH ~/bash/bin $PATH
